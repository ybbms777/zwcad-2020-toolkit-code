# 更新记录

最新在最上面。

## 1.2.39 — 2026-09-21
- 修复：真实图纸里 TR / EX 悬停高亮完全不亮 —— 中望 CAD 的选择集接口收的是 UCS 点，图纸的 UCS 原点不在 (0,0) 时按 WCS 传点一个对象都选不到，命中恒为 0（合成测试图纸 UCS=WCS，所以之前没暴露出来）
- 注意：更新后需要重启中望 CAD，插件才会加载新的 bin/ZWKit.Core.102.dll（DLL 无法热替换）

## 1.2.38 — 2026-09-21
- 使用说明：TR / EX 一段改成实际行为 —— 原生准星、只亮会被剪掉的那一段、Shift 是延伸（整条亮）
- 使用说明：明确写清 TR / EX 期间不支持滚轮缩放与中键平移（用的是 CAD 自己的输入循环，收不到滚轮），要调视图先 Esc 退出、调好再执行

## 1.2.37 — 2026-09-21
- TR / EX：悬停高亮只亮「点下去会被剪掉的那一段」（到左右最近两个交点的区间），不再是整条线
- TR / EX：按住 Shift（延伸）时不预览那一段，整条亮（延伸的那一段要按边界算，暂未做）
- DLL：新增 ZWK_BOUND_101（边界集合句柄表），快速模式传空串 = 全部对象都算边界
- DLL：ZWK_HOVER_101 多一个 shift 参数；带 ZWK_HOVERCHECK_101 的自检也走同一套逻辑
- 速度：悬停只在压到的对象附近开小窗找交点（不整图扫描），并按「对象 + 区间」签名决定要不要重画

## 1.2.36 — 2026-09-21
- TR / EX：改用 CAD 自己的 grread 输入循环，准星就是 CAD 原生那一个（自己起消息循环时它必然僵在原地）
- TR / EX：删掉自绘光标和「盖住冻结准星」的贴纸，不再有那个不动、看着别扭的方块
- TR / EX：悬停高亮（压到哪条线哪条线变青色）改用置顶透明窗绘制，真机上看得见
- TR / EX：拖动修剪 / 按住 Shift 拖动延伸的笔画（青 / 红）同样走透明窗，不再被画布重绘冲掉
- DLL：ZWKitMouse 补上 CommandClass 注册 —— ZWK_*_101 这组 LispFunction 以前根本没注册上（只注册了 ZWKCAP102/103 这些命令）
- 修复：ZWK_SHIFT_101 等无参接口不能传空参数表，LISP 侧统一按一个元素的表拆包
- 修复：TR / EX 结束（含 Esc）时 CURSORSIZE 会还原成用户原来的值
- 排查：设环境变量 ZWK_TRIMDEBUG=1 后，TR / EX 的每步输入会写进 %TEMP%\zwk-trim-debug.txt

## 1.2.35 — 2026-09-21
- TR / EX：自绘光标改成 AutoCAD 的形态——只有一个中间小方框，跟着鼠标走
- TR / EX：把 CAD 冻住的那个准星盖掉（CAD 在命令执行期间不处理鼠标，自带准星必然僵在原地），不再出现两个准星
- TR / EX：悬停高亮（青色描边）与 CAD 自身虚线高亮保留
- 拖动修剪 / 拖动延伸（ZT / TRD / EXD）行为保持不变

## 1.2.34 — 2026-09-21
- TR / EX：不再出现两个准星——鼠标移动交还给 CAD，CAD 自己的准星跟着鼠标走，不再自绘光标
- TR / EX：进入命令时把 CURSORSIZE 临时设为 1，准星只剩中间那个小方块（与 AutoCAD 一致），退出（含报错退出）自动还原
- TR / EX：鼠标压到线上时给出高亮反馈——青色描边贴在实体上，同时 CAD 自身的虚线高亮也生效
- 拖动修剪 / 拖动延伸（ZT / TRD / EXD）行为保持不变

## 1.2.33 — 2026-09-21
- 修复 TRD / EXD 一直是「未知命令」：click.lsp 用 (setq c:TRD c:TR) 给命令做别名，而 ZWCAD 的命令查找只认真正的 defun —— 变量有值、命令却不注册，敲 TRD / EXD 直接报未知命令。这两个命令从上线起就没生效过（交接文档里「ZT / TRD 都是拖动版」的说法在真机上从来没验过）。现在改成在 trim.lsp / extend.lsp 里显式 defun；模块被取消勾选时 boot.lsp 也会把它们一起清掉
- TR / EX 补上「删除无法修剪的选定对象」：这是 AutoCAD 快速模式的既定行为（官方规格里明确写了「选择不与边界相交的对象会删除该对象」），而 ZWCAD 原生不会删 —— 真机实测：不可修剪的对象原样留着、也不报任何错。现在按「操作前后实体数据是否变化」判断，没被修剪到的就删掉，与 AutoCAD 一致
- TR / EX 的单个选择改用「裸点」喂原生命令，不再用 2 像素小窗交：真机实测 ZWCAD 不会把裸点当成矩形框选（无挂起命令、剪掉的那一段也对），而且裸点能让拾取点自己决定剪哪一端，比小窗交更贴近 AutoCAD
- 修一个改动过程中引入的 bug：快照必须在执行「前」拍。写成执行后再拍，就变成自己跟自己比，永远判成「没修剪到」，把修剪成功的对象也一起删掉 —— 真机测试抓到的

## 1.2.32 — 2026-09-21
- 修复 TR / EX 实际无法修剪 / 延伸（点击、滑动、Shift 全都无效）：根因是给原生命令传边界时用了 ""（回车=全选）。在 ZWCAD 2020 上实测，TRIM / EXTEND 收到 "" 会「从头到尾正常走完、不报任何错、也不改图形」；改成传真实选择集 (ssget "_X") 后，栏选 / 窗交 × 修剪 / 延伸 四种组合全部生效（已实机逐项验证）
- 排查留痕（便于以后不再踩）：同一张测试图、同一批点，V1 复刻拖动版写法 / V2 一次性喂全部参数 / V3 不带 _non / V5 窗交，这四种传 "" 的写法全部无变化；V4 与 A1~A4 传选择集的写法全部生效 —— 差异只有「边界参数」这一个变量
- 已知同类问题（本次未改，待确认后再动）：拖动版 modules/trim.lsp（ZT / TRD）与 modules/extend.lsp（EXD）也用了「边界传 ""」的写法，所以它们的修剪 / 延伸同样不生效，目前是靠 trim.lsp 里「没被修剪的对象整根删掉」的兜底表现为「线消失了」。要改的话是同一处一行改动，但会改变 ZT 的既有行为，所以先留着等确认

## 1.2.29 — 2026-09-19
- 修复「更新.cmd 无法连接更新源」：update.ps1 的更新源数组写法错 —— 写成 @(a + $x, b + $y)，而 PowerShell 里逗号的优先级高于加号，结果只得到 1 个元素（两个更新源地址被空格拼成一个字符串），请求变成 404。改为先把每项算成变量再组数组
- 修复「更新.cmd 无法连接更新源」：WebClient.DownloadString 默认按系统 ANSI（中文机上是 gb2312）解码，而 version.json 是 UTF-8 —— 里面的中文文件名会变乱码，ConvertFrom-Json 解析直接失败，$manifest 为空就报「无法连接」。改为显式指定 UTF-8（服务器虽然返回 charset=utf-8，但 DownloadString 不看响应头，必须手动设）
- 修复「更新.cmd / 发布.cmd 报 ‘ZWCAD’ 不是内部或外部命令」：这两个文件是 LF 行尾，而 cmd.exe 需要 CRLF —— LF 行尾遇到中文 title 会让 cmd 解析错位。四个 .cmd 统一改为 CRLF
- 更新器失败时不再静默：取清单失败会打印共几个更新源、每个源的具体结果（原来只报「无法连接更新源」，分不清是网络、404 还是 JSON 解析问题）

## 1.2.28 — 2026-09-19
- 修复图框编辑器多页图纸页码方向反了：最右边的图框被编成「第 1 页」（tke:autopage 里多了一次 reverse）。现在按「从左到右 = 第 1 页」编号
- 修复 ZWKCHECK 在插件 DLL 未加载时报错：平滑度取值为空时直接 (itoa nil) 会报错，而用户敲这条命令往往正是为了排查「插件没加载」。现在取不到就显示「未知」；版本号也改为从 version.json 读取（原来写死 1.2）
- 修复图框编辑器多图框时「A=取消」失效：按 A 和按回车都返回空，被一起当成「全部一起改」。现在按 A 会明确取消
- 图框编辑器写入统计改为真实成功数：原来 entmod 写失败只返回 nil 不报错，却仍被计为成功，「已写入 N / M」会虚高让人以为全写进去了
- 发布器新增「公开仓库同步后自检」：逐文件比 sha256 + 校验 version.json 同版本 + 检查无敏感文件，不通过就中止且不推送。原来只打印期望文件数，一旦复制失败也会显示「已同步 N 个文件」，公开仓库残缺而无人察觉（上次漏 20 个文件就是这么来的）
- 更新.cmd / 发布.cmd 改为 GBK 编码（原来是 UTF-8，cmd 窗口标题里的中文会乱码）
- 加固：取对象包围盒、强制填满的非等比缩放都补了 catch-all，单个对象失败不再拖垮整批、也不再留下「半变换」状态；余量输入非数字不再静默变成 0

## 1.2.27 - 2026-09-19
- 更新器（更新.cmd）改为「校验通过才算下载成功」：每个更新源各试一次，哈希不符就换下一个源；两个源都不符就等一会儿整轮重试（最多 3 轮，分别等 15/30 秒）。解决 GitHub 的 raw CDN「按文件缓存」导致的假失败
- 更新器拉清单时两个源各拉一次，取 date 更新的那一份 —— 清单本身也会被 CDN 缓存，只从一个源拉会误报「已是最新」
- 把误导性的「校验不通过（文件可能不完整）」改成「更新源还在发旧文件（GitHub 的 CDN 缓存），过几分钟再跑一次本更新即可」
- 「已是最新」时补一句提示：如果刚发布完却显示已是最新，可能是更新源缓存没刷新，过几分钟再跑一次
- 本地 version.json 直接写已取到的那份清单，不再多发一次请求

## 1.2.26 - 2026-09-19
- 同步 使用说明.md：样品数量的提示改为「只输数字，单位 pcs」（输入 5 写回 5pcs），并移除已去掉的图纸日期那一行（1.2.25 改了代码但漏改文档）

## 1.2.25 - 2026-09-19
- TKE 样品数量改为只输数字：输入 3 自动写回 3pcs。默认值显示时会剥掉 pcs 后缀，非纯数字会提示重输
- TKE 移除「图纸日期」：本命令不再修改日期（角色表和提问流程都已去掉）
- TKE 字段提问改为走一张表（tke:asks），以后增删字段只改这一处

## 1.2.24 - 2026-09-18
- ★ 修复 TKE 写入报「undefined function - SETCAR」：ZWCAD 的 LISP 没有 setcar 这个函数，导致整批写入在第一个属性后就中断（只写进去一个值，且 UNDO _End 没执行到）
- TKE 去掉 setcar：写入流程没有「写后再读」的需求，不需要回写内存副本
- TKE 每个属性单独包 catch-all：某个属性写失败不再中断整批，也不会让 UNDO _End 执行不到（避免用户的选择集被留在未结束的 UNDO 组里）
- TKE 写入后显示「已写入 N / M 个属性值」，有失败会明确警告，不再只报「已写入 N 项」

## 1.2.23 - 2026-09-18
- ★ 修复 TKE 读不到块属性（根因）：ZWCAD 2020 上 vla-get-attributes 这个方法一个属性都读不出来（块名认得对、属性数=0），导致「没找到图框」
- TKE 改用纯 DXF 读块属性：属性就排在 INSERT 后面，用 entnext 一路走遇到非 ATTRIB 停。完全不依赖 ActiveX，兼容性最好
- TKE 读属性改为三条路依次试：① DXF entnext ② 全图找 ATTRIB 按宿主块（330 组码）匹配 ③ ActiveX（仅备用），成功的那条会打印出来
- TKE 写属性同样兼容两种：属性实体是实体名走 entmod，是 VLA 对象走 vla-put-textstring
- TKE 读不出属性时打印原因：块的 66 组值 + entnext 后面第一个实体的类型，便于一眼定位

## 1.2.22 — 2026-09-18
- 修复 TKE 找不到图框：原来 ssget "X" 带了 (66 . 1)「有属性」过滤，在 ZWCAD 上一个块都选不到，直接报「没找到图框」
- TKE 改为扫描全部布局（按 (410 . 布局名) 逐个搜），图框在别的布局里也能找到
- TKE 不再用 vla-get-hasattributes 当门槛（该属性在部分 CAD 上取不到，会把全部块判成没属性），改为直接读属性看结果
- TKE 支持一张图纸多个图框：找到多个时可选「全部一起改」，共几页 / 第几页 按左右顺序自动编号；也可点选只改一个
- TKE 增加诊断输出：扫描到多少块参照 / 带属性 / 认出图框；找不到时打印块名或带属性块的标记名，便于定位模板差异
- TKE 判断左右顺序改用块插入点（DXF 组码 10），不再依赖 ActiveX 取包围盒

## 1.2.21 — 2026-09-18
- 新增第 9 个模块「图框编辑器」TKE / BKE：只针对样品图。料号只输一次数字，自动填 客户编号 KY-xxx / 产品编号 样品-xxx / 产品料号 01000xxx 三处，解决三处数字相同却要分别输入、容易漏改的问题
- TKE 还能改 图名（客户料号）/ 材质 / 负责人 / 样品数量 / 版本 / 图纸日期 / 共几页 / 第几页，每项回车即保持原值；只列真正变化的字段供确认；写入合成为一步，按 U 可整体撤销
- TKE 自动扫描图纸里的块找出图框（认「料号」「图名」属性），找不到或多个时改为点选
- 安装菜单新增 [9] 图框编辑器，全部安装移到 [10]，字体安装移到 [11]

## 1.2.20 — 2026-09-18
- TK 零件包围盒改为取 4 个极值点在 X / Y 两个方向的真实最小/最大。原来只用「最左/最右」定 X、「最下/最上」定 Y，视图或 UCS 有旋转时这两个方向对不上，算出的框会偏小或错位
- TK 预览新增留白数值报告：显示「缩放后可用区」和「左右各余 / 上下各余 xx mm」，空白在哪、有多少一目了然
- TK 新增 F=强制填满：图框按 X / Y 分别缩放（变换矩阵实现，任意对象类型可用），四边精确零留白；代价是图框上的文字和圆会被拉变形，执行前有明确警告
- TK 候选可用区改为按「留白最少」挑（原来按「缩放系数最小」挑，缩放最小只代表框大，长宽比不对时框越大留白越多）

## 1.2.19 — 2026-09-18
- TK 修复「参数太少」报错：tk:mag 加了颜色参数后，tk:dot 还在用旧写法调用，参数不够直接报错，导致第 4 步的零件范围汇总完全不显示
- TK 可用区改为按零件尺寸挑最贴合的候选：原来只取面积最大的空矩形，但面积最大不一定最省料（例：右上角有表时，面积最大是「整宽×表下方」，而「表左侧×整高」装同一零件缩放系数更小、留白更少）。现在把遮挡物切出的所有空矩形都作为候选，第 5 步知道零件尺寸后自动挑缩放系数最小的那个
- TK 零件在可用区内四边均匀分布：原来按「可用区左边=零件左边-左余量」定位，富余空间全堆在右侧。现在把富余空间平分到对边，不再出现单侧大量留白

## 1.2.18 — 2026-09-18
- TK 第 3 步可视化：每圈一块遮挡物立刻画黄框，算出的可用区画蓝框，并打印「内边框 vs 可用区」尺寸对比
- TK 第 3 步新增确认/重圈：算完可用区问「可用区对吗？[回车=是 / R=重新圈]」，圈歪了不用重跑整个命令
- 修复 TK 第 4 步「零件最左」不打印已记录：最左点走的是另一个取点函数，漏了这行打印，让人以为点击没被记录

## 1.2.17 — 2026-09-18
- TK 第 3 步改为「圈出遮挡物」：点两个对角点圈一个矩形，可圈多块。不再用框选对象
- 解决「整个图框是一个块，里面的标题栏/变更记录表选不上」：框选永远只能选中整个图框块，范围等于整张图，等于没减。圈区域不受块、锁定图层、外部参照影响
- TK 第 4 步去掉重复的「零件最左」提示：原来 [4/5] 和下一步各提示一次，会让人以为第一次点击没被记录

## 1.2.16 — 2026-09-18
- 修复致命 bug：tk:usable 返回的是矩形，而下游 tk:confirm / tk:apply 要的是「宽 高 中心点」，导致第 5 步设完余量后报「类型不正确 - (x y 0.0)」直接中断
- 新增类型守卫：可用区 / 零件范围结构不对时给出明确提示，而不是抛晦涩的类型错误
- TK 第 3 步新增「直接点两点手动指定可用区」：回车走遮挡物自动算，直接点两点则手动指定。遮挡物框选不上时（锁定图层 / 外部参照 / 和整个图框在同一个块里）用这个
- TK 第 4 步输出改清楚：分别列出你点的 4 个位置，再列出合成后的包围盒，避免「左 X 和我点的最左点不一样」的困惑

## 1.2.15 — 2026-09-18
- 修复对象捕捉被永久关闭：tk:getpt 里恢复 OSMODE 的语句写在 getpoint 之后，中途按 Esc 时执行不到，导致整个 CAD 会话的对象捕捉一直是关的
- 现在命令开始时保存 OSMODE / CMDECHO 原值，正常结束和出错（错误处理函数）都会恢复
- 如果你已经中招（捕捉没了）：命令行执行 (setvar "OSMODE" 4133) 回车，或按 F3 重新打开对象捕捉

## 1.2.14 — 2026-09-18
- 修复坐标系混用：getpoint/getcorner 返回 UCS，而 vla-getboundingbox 返回 WCS。内部混着用导致辅助框画到很远的地方、内边框尺寸算错、遮挡物被误判到框外
- 现在内部一律用 WCS：取点后先转 WCS 再算，传给 command 前再转回 UCS
- TK 第 3 步增加提示：框选到 N 块、其中落在内边框里的有 M 块；全在框外时给出明确提示
- 无遮挡物时不再误报「可用区算出异常」

## 1.2.13 — 2026-09-18
- 修复 tk:clip 参数名 r / R 重复：AutoLISP 符号名不区分大小写，两者是同一个名字，ZWCAD 报「参数名重复」并让整个 defun 定义失败，连带 frame.lsp 后面的函数全部没定义（表现为 TK 未知命令）
- 已扫描全部模块，确认只有这一处大小写不敏感重名问题

## 1.2.12 — 2026-09-18
- 修复 TK 可用区算出跑到内边框外面（旧版出现「可用区 396 x 1616」，比内框还高）：最大空矩形算法现在先把遮挡物裁进内边框，完全在框外的遮挡物直接丢弃，并加了兜底校验
- TK 点零件极值改为关闭对象捕捉，直接用准星原始位置；每点一下回显记录的坐标
- TK 余量改为 mm 单位、上下左右分别设置（不再用百分比）；缩放前会先问是否留余量
- TK 缩放后按四边余量精确落位：上/下/左/右各留对应距离，而不是简单居中
- 框选后自动清空选择集，避免对象一直处于高亮状态干扰下一步框选

## 1.2.11 — 2026-09-18
- 修复 TK 框选遮挡物后报「函数参数类型不正确」：矩形是「点的点对」，多处直接 (car 矩形) 取到的是点不是坐标，导致 min/max/vl-sort 报错
- 新增 tk:rx1/ry1/rx2/ry2 四个矩形取值函数，所有坐标访问一律走它们，杜绝这类错误
- 修复出错时错误处理函数自身再报错的问题（清理动作包了 catch-all）

## 1.2.10 — 2026-09-18
- TK 新增第 3 步「框选图框内遮挡物」：把标题栏、变更记录表等占地方的对象框进去，程序自动算出可用区
- 可用区用「最大空矩形」算法算：不是简单取内框减遮挡物并集（遮挡物一个在底部一个在右上角时并集等于整个内框，等于没减）
- 可用区会用洋红框画出来，零件只装在这个框里，不会再压到标题栏或变更记录表
- TK 流程改为 5 步：[1]选图框 [2]点内边框 [3]框选遮挡物 [4]点零件 4 个极值 [5]确认执行

## 1.2.9 — 2026-09-18
- TK 标极值改为智能判定：点空白处用点坐标，点在对象上则取该对象包围盒的边缘
- 修复文字/标注类对象点不准边缘的问题：文字没有可捕捉的右端点，现在直接点在文字上就取它的最右边缘
- TK 辅助框的 4 个圆点改画在包围盒四条边的中点上，位置更直观

## 1.2.8 — 2026-09-18
- TK 第 2 步改为「直接点内边框角点」：原来要先打字 I 再回车，直接点屏幕没反应
- TK 第 3 步改为按建议点 4 个极值点（最左/最右/最下/最上）标出零件范围，回车可回退为框选自动量
- 修复洋红辅助框不显示：grdraw 画的临时线会被重绘冲掉，改用真实实体（用完自动删除），并在点的 4 个位置各画一个洋红圆点
- 修复图框缩放后没对齐到零件：vla-getboundingbox 返回 WCS 坐标而 command 按 UCS 解释点，现在所有点先转 UCS
- TK 对齐改用位移量计算，不再依赖 SCALE 基点与 MOVE 的基点语义

## 1.2.7 — 2026-09-18
- TK 标题框缩放改为自动包裹：图框缩放后自动移到零件正中，不用再手动 MOVE 拖过去
- TK 选零件时新增洋红虚线框标出量到的范围，并打印「最左 X / 最右 X / 最下 Y / 最上 Y」四个坐标，确认贴住零件最外沿再回车；没框全可按 R 重新框选
- TK 流程重排为 [1/4] 选图框 → [2/4] 定可绘图区 → [3/4] 选零件 → [4/4] 确认执行，每步都有编号和说明，开头加了 4 步概览
- TK 零件尺寸改为「先自动量、再允许覆盖」：回车用自动量到的值，也可输入 宽,高 覆盖

## 1.2.6 — 2026-09-18
- 新增「标题框缩放」模块（命令 TK / BTK）：把图框按零件尺寸自动缩放并移到零件正中，零件不动
- 标题框缩放支持两种尺寸输入：手输（如 105.95,117.9，逗号/空格/x 分隔都行）或框选零件自动量取
- 标题框缩放支持两种规则：精确贴合（按实际尺寸）与标准比例（吸附到 1:100/1:50/1:10/1:2/1:1/2:1 等系列），可设留边余量
- 修复安装菜单编号错位：原来 [5] 和 [7] 的模块映射是反的（选 [5] 改字体 实际装的是机械符号）
- 安装菜单调整为 [1]-[8] 为可选模块、[9] 全部安装、[10] 字体安装

## 1.2.5 — 2026-09-18
- CLI 标题框的版本号改为从 version.json 动态读取，不再写死 v1.2，以后发布自动跟随最新版本

## 1.2.4 — 2026-09-18
- 修复完整包内容：不再包含 selection.lsp（模块勾选属于本机配置，安装时会重新生成，不该随包分发）

## 1.2.3 — 2026-09-18
- 修复字体指纹算法：改用与文件顺序无关的 XOR 累加，避免不同工具算出的指纹不一致导致每次发布都误判"字体有变化"
- 修复 version.json 里 CHANGELOG.md 的哈希陈旧问题（先追加更新说明、再计算指纹）

## 1.2.2 — 2026-09-18
- 发布器（发布.cmd / publish.ps1）不再随包分发：从更新清单、公开仓库、完整包中移除，只保留在作者本机
- 发布器新增发布权限校验：必须存在作者本机的发布用 SSH 密钥才能运行，其他人拿到也跑不起来

## 1.2.1 — 2026-09-18
- 新增云更新：双击 更新.cmd 即可升级，只下载发生变化的文件（通常几十 KB），无需重新发压缩包、无需重新安装
- 新增 发布.cmd / publish.ps1：一键重算文件指纹、推送到公开与私密两个仓库、并重打完整包
- 新增版本清单 version.json：记录每个文件的 sha256、字体版本号、本版更新说明
- 修复 CLI 标题框在 cmd.exe 里右侧错位（框线字符在 GBK 控制台占 2 列，改用纯 ASCII 边框）
- 修复字体列表把小于 50 KB 的字体显示成 0.0 MB（改为按 B / KB / MB 自动选单位）
- 补齐缺失字体 bigfont.shx（日文大字库，3664 字形，已解析验证）
- 修复 fonts\字体说明.txt 混合编码导致的乱码，改为 UTF-8 with BOM

