# 中望 CAD 2020 整合工具包 1.2

包含智能标注、拖动修剪/延伸、几何公差、机械符号、改字体、标注线属性，并自动设置圆弧平滑度、随包安装常用字体。适用于中望 CAD 2020 中文版、二维模型空间。

## 安装（只需一次）

1. 把整个"ZWCAD工具包"文件夹放在固定位置，不要移动或删除。
2. 退出 CAD 中正在执行的命令（安装脚本不会帮你关闭/保存图纸）。
3. 双击"一键安装.cmd"，菜单选 **10 全部安装**（也可按需单独选 1-9 中的功能，逗号分隔多选，如 `1,2,5`）。
4. 以后打开 CAD、新建或打开图纸会自动加载，无需再手动操作。

安装菜单分三类：**[1]智能标注 [2]快速修剪 [3]快速延伸 [4]几何公差**（标注与绘图）、**[5]改字体 [6]标注线属性 [7]机械符号**（文字与标注线）、**[8]标题框缩放 [9]图框编辑器**（图纸整理），外加 **[10]全部安装**、**[11]字体安装**（独立于插件功能，随时可单独补装）。点选式 TR/EX/栏选删除(FE) 已内置在基础组件里，不需要单独选。

安装失败可查看本目录 `install.log`；CAD 内输入 `ZWKCHECK` 可检查模块状态。

## 更新（有新功能时）

双击 **`更新.cmd`** 即可。它会联网比对，**只下载发生变化的文件**（通常 1-3 个、几十 KB，几秒完成），
不需要重新安装、不需要重新发压缩包、字体也不会重复下载。

- 建议先关掉 CAD 再更新（插件 DLL 被占用时无法替换，脚本会提示）
- 更新完重新打开 CAD 生效
- 旧文件会备份到 `_backup\日期时间\`，出问题可以拷回来
- 你自己的模块勾选（`selection.lsp`）不会被覆盖

## 常用命令

|命令|用途|
|---|---|
|ZD、ZD2|点两个点标水平/垂直距离；回车改为选对象|
|ZDO|选直线标长度、圆标直径、圆弧标半径|
|ZDJ|选两条直线标角度|
|ZDJ3|选顶点和两边上的点标角度|
|ZDB|点两侧直边，标宽度/高度|
|TR|点选修剪：点 A 点 B 即剪；回车结束；U 撤销上一笔|
|EX|点选延伸：点 A 点 B 即延；回车结束；按住 Shift 改为修剪|
|ZT|拖动修剪：按住左键划过，松开执行|
|EXD|拖动延伸：划过延伸，按住 Shift 拖为修剪|
|FE|栏选删除：点 A 点 B，穿过的线整根删除；U 撤销|
|GC|几何公差：打开CAD原生对话框，填好点位置放置|
|FH|机械符号：选 1 沉孔/2 深度/3 直径/4 沉头，再点文字插入|
|ZF|改字体：选字体（1 宋体/2 华文细黑/3 Arial）→ 选字高 → 选颜色（可选不改）→ 连续点选标注/文字；期间随时 T 换字体、H 换字高、Y 改颜色|
|ZFD|改字体（批量）：选字体、字高、颜色后连续框选/交叉/栏选（不间断）；空回车（不选东西）会问要不要 T/H/Y 改设置或结束|
|BZ|改标注线：选颜色（默认绿）/箭头大小（默认1.8）/线宽（默认0.05mm）→ 连续点选标注；期间随时 Y 改颜色、S 改箭头、W 改线宽。只改标注线本身（线/箭头），不影响标注文字|
|BZD|改标注线（批量）：同 BZ 的设置，连续框选/交叉/栏选（不间断）；空回车（不选东西）会问要不要 Y/S/W 改设置或结束|
|TK|标题框缩放：框选图框 → 框选零件 → 回车。图框自动缩放并包裹到零件上，量完会用洋红虚线框标出范围供核对|
|TKE|图框编辑器：料号只输一次数字，自动填 客户编号 KY-xxx / 产品编号 样品-xxx / 产品料号 01000xxx 三处。**仅样品图**|
|ZWKCHECK|查看模块状态和当前平滑度|

**小贴士**：TR / EX 用 CAD 原生准星，压到哪条线，**会被剪掉的那一段**就亮青色（不是整条线）；按住 Shift 变成延伸、整条亮。EX 拖动时青色=延伸、橙红色=Shift修剪。TR / EX 执行期间不支持滚轮缩放和中键平移（用的是 CAD 自己的输入循环，收不到滚轮），要调视图先按 Esc 退出、调好再执行。ZF/ZFD 只改动点到（或框选到）的对象，不影响标注样式，公差小字和工程符号（Ø、沉孔等）会自动保留不被替换。

**字高预设**：1=1.5、2=2、3=2.5、4=3、5=4、6=8、7=10、8=自定义（也可直接输入任意数字）。

**颜色**：开始时会问一次颜色，选 0（或直接回车，若之前选过则默认为上次的选择）表示不改颜色；其余选项：1红/2黄/3绿/4青/5蓝/6白或黑/7随层(ByLayer)/8自定义编号（0=随块，1-255=标准色）。设置后在整个命令运行期间随时可按 Y 重新选择，直到回车结束命令。**对标注只改文字颜色，尺寸线、尺寸界线、箭头颜色不受影响。**

（换字体/换字高/换颜色的中途快捷键是 T/H/Y，改标注线的是 Y/S/W，没有用常见的 F/C，是因为框选/交叉/栏选本身就要用到 F(栏选)、C(交叉选择) 这两个字母。另外 ZWCAD 的"选择对象:"提示本身不认自定义关键字——ZF/BZ 逐个点选没问题；ZFD/BZD 批量框选可以连续多次框选不间断，只有在框选提示里直接回车（不选任何东西）时才会弹出设置菜单，这是 CAD 本身的限制，不是没做好。）

**BZ/BZD 的颜色/箭头/线宽**：只作用于标注本身的线、尺寸界线和箭头（用标注局部覆盖实现，不动标注样式，也不影响 ZF/ZFD 改的文字颜色），彼此互不干扰，可以同一个标注上又用 ZF 改文字颜色、又用 BZ 改线的颜色。颜色同样支持 0=不改；线宽单位是毫米。

## 标题框缩放（TK）

**作用**：把图框缩放并套到零件上。**零件不用动，图框会自己缩放并套上去。**

> 一句话记法：**选图框 → 点内边框 → 框选遮挡物 → 点零件 4 个极值 → 设余量 → 自动套上**。

### 五步走（每步命令行都有提示）

**第 1 步 · 选图框**
框选整个图框（外边框 + 标题栏一起），回车结束。会打印「图框外框 = xxx x xxx」。

**第 2 步 · 点内边框**
```
[2/5] 点【图框内边框】第一个角点（回车=改用图框外框，快）:
      点对角:
```
**直接点**图框内边框的一个角点，再点对角。回车则拿外框当内边框。

**第 3 步 · 圈出遮挡物（关键）**
```
[3/5] 圈出【图框内遮挡物】（标题栏、变更记录表等）
      点遮挡物的两个对角点；可以圈多块；没有遮挡物就直接回车
      点第 1 块遮挡物的第一个角点（回车=没有遮挡物）:
      点对角:
      已圈第 1 块（黄框，继续圈或回车结束）
      继续圈下一块（回车=圈完）:
      点对角:
      已圈第 2 块（黄框，继续圈或回车结束）
      继续圈下一块（回车=圈完）:    ← 直接回车结束
      圈了 2 块，落在内边框里的有 2 块
      内边框 = 440.756 x 247.847
      候选可用区 6 个（第 5 步知道零件尺寸后自动挑最贴合的）
      其中最大的 = 396.000 x 245.000
      黄框 = 你圈的遮挡物，蓝框 = 最大的那个候选
      可用区对吗？[回车=是 / R=重新圈]:
```

> **为什么是「候选」而不是直接定一个？**
> 遮挡物把内框切出来的空区域不止一块。面积最大的那块**不一定最贴合你的零件**。
> 例：右上角有个表，最大空矩形是「整宽 × 表下方」，但「表左侧 × 整高」虽然面积小一点，
> 装同一个零件时需要的缩放系数更小（**框更小、留白更少**）。
> 所以程序把所有候选都留着，等第 5 步知道零件尺寸后自动挑最省料的那块。

**屏幕上会实时画出**：

| 颜色 | 含义 |
|---|---|
| **黄框** | 你圈出的遮挡物（每圈一块立刻画出来） |
| **蓝框** | 算出的可用区 —— **零件要装进这个框里** |
| 洋红框（下一步） | 零件范围 |

**圈完先看三个数**：`内边框` 和 `可用区` 会一起打印出来。
如果可用区明显比内边框小很多，说明圈多了或者圈歪了 —— 按 **`R`** 重新圈，不用重跑整个命令。

> **为什么用「圈区域」而不是「框选对象」？**
> 因为整个图框通常是一个**块**，里面的标题栏和变更记录表**选不上**——
> 你一框选就选中整个图框块，它的范围等于整张图，等于没减。
> 圈区域不受块、锁定图层、外部参照影响，永远能用。
>
> 圈的区域超出内边框时会先裁进内框再算，不会算出跑到框外的可用区。
> 「圈了 N 块，落在内边框里的有 M 块」两个数应该一样；M 是 0 说明圈到内框外面去了。

**第 4 步 · 点零件 4 个极值（关捕捉）**
```
[4/5] 点【零件最左】位置（关捕捉，用准星；回车=改为框选零件自动量）:
      点【零件最右】位置:
      点【零件最下】位置:
      点【零件最上】位置:
```

**这一步会临时关掉对象捕捉**，用的就是你准星的原始位置 —— 看到位置合适直接点就行。
每点一下会回显记录的坐标：

```
        已记录 3161.122 , 452.852
```

**四个位置每点一下都会打印「已记录 X , Y」**（包括最左那一下），
方便你逐个核对。点完四个位置后会看到：

```
      -- 零件范围已用洋红框标出（4 个圆点在四条边中点上）--
        你点的 4 个位置：
          最左点  3362.823 , 556.631
          最右点  3502.361 , 645.571
          最下点  3362.823 , 447.256
          最上点  3158.785 , 645.571
        合成后的包围盒（取 4 个点的最小/最大）：
          左 X = 3158.785   右 X = 3502.361
          下 Y = 447.256   上 Y = 645.571
          宽 x 高 = 343.576 x 198.315
      贴住零件最外沿了吗？[回车=是 / R=重新点]:
```

> **为什么「左 X」和我点的「最左点」不一样？**
> 因为图框是按 **4 个点合成的包围盒** 来算的（取 X 的最小/最大值、Y 的最小/最大值）。
> 如果零件在图上转了 90°，「屏幕上的最上」在坐标里可能是「X 最小」的那个点。
> 对照一下上面「你点的 4 个位置」，就知道程序取的是哪几个值了。

**屏幕上会画一个洋红框套住零件**，确认没问题就回车；不对按 **`R`** 重来。

> 想省事就在第 4 步提示处**直接回车**，切换成「框选零件自动量」，不用点 4 下。

**第 5 步 · 设余量并执行**
```
[5/5] 零件尺寸 <343.969 x 188.098> [回车=用这个 / 或输入 宽,高 覆盖]:
      要留余量吗？[回车=不留（零件贴边）/ Y=留（按 mm 分上下左右设）]:
```
- 直接回车 = 用刚标出的尺寸
- 也可以输入覆盖，如 `105.95,117.9`（逗号 / 空格 / `x` 分隔都行）

**如果选 `Y` 留余量**，会依次问四边，**单位 mm**：
```
      输入四边余量，单位 mm（直接回车用默认 5）:
        上余量 mm <5>:
        下余量 mm <5>:
        左余量 mm <5>:
        右余量 mm <5>:
      余量：上 5.000 / 下 5.000 / 左 5.000 / 右 5.000 mm
```

然后显示预览：
```
      已按零件尺寸从 6 个候选里挑出留白最少的可用区 396.000 x 245.000
      可用区    396.000 x 245.000
      零件尺寸  343.969 x 188.098
      余量 mm   上 5.000 下 5.000 左 5.000 右 5.000
      缩放系数  0.900   [精确贴合]
      缩放后可用区 356.400 x 220.500
      左右各余 11.215 mm（合计 22.430）
      上下贴紧
      [回车=执行 / S=切标准比例 / M=重设余量 / F=强制填满 / Esc=取消]:
```

**留白报告会明确告诉你空白在哪、有多少**（`左右各余` / `上下各余`）。程序把富余
**平分到对边**，所以不会出现「一边贴死、另一边大量留白」。

- **回车** = 执行，图框自动套到零件上
- **`S`** = 在「精确贴合」和「标准比例」之间切换
- **`M`** = 重新设置四边余量
- **`F`** = **强制填满**（四边零留白，见下）
- **`Esc`** = 取消

#### 为什么等比缩放一定会有留白

图框和零件的**长宽比不同**，等比缩放没法同时让两边都贴紧：

| | 宽 | 高 | 长宽比 |
|---|---|---|---|
| 可用区（缩放后） | 356.400 | 220.500 | 1.616 |
| 零件 | 343.969 | 188.098 | 1.829 |

零件比可用区「更扁」，等比缩放到高度贴紧时，宽度就必然多出 22.43 的空白。
**这是数学上的必然，不是 bug。** 想消掉只有两条路：

1. **回第 3 步圈一个长宽比更接近零件的可用区** —— 推荐，图框形状不变形
2. **按 `F` 强制填满** —— 程序按 X / Y 分别缩放图框，四边精确零留白。
   **代价：图框上的文字、圆、箭头会被拉变形**，执行后请检查

> 有留白时程序会自动提示「留白偏多」，并给出这两个选项。

执行后图框会被缩放并**按你设的四边余量套到零件上**，零件一点没动。随时可以 `U` 撤销。

### 两种缩放规则

- **精确贴合**（默认）—— 按零件实际尺寸 + 四边余量算，想多紧就多紧
- **标准比例** —— 吸附到标准比例系列（1:100 / 1:50 / 1:20 / 1:10 / 1:5 / 1:2 / 1:1 / 2:1 / 5:1 …），
  取不小于精确值的那个（保证装得下），但可能留较多空白

### 常见疑问

- **洋红框和圆点没出现？** 它们只是辅助显示。**坐标数字才是准的**，对照图纸核一下就行。
- **图框缩放后两边还有空白？** 等比缩放的必然结果（图框和零件长宽比不同）。
  回第 3 步圈个长宽比更接近零件的可用区，或按 `F` 强制填满。
- **零件框没贴住零件？** 1.2.20 起包围盒取 4 个点在 X / Y 两个方向的**真实**最小/最大，
  视图旋转过也准。若仍不准，把命令行打印的 4 个点坐标发出来。
- **第 3 步漏了遮挡物怎么办？** 重跑一次 `TK` 补上；不框的话零件可能压到标题栏。
- **第 4 步点不准怎么办？** 这一步不吸附，用的就是准星原始位置；不满意按 `R` 重点 4 下。
- **想反悔？** 执行后按 `U`，图框会退回原位和原大小。

## 图框编辑器（TKE）

> **只针对【样品图】的图框，量产图暂不支持。**

**解决的问题**：公司料号要在图框的三个地方输入，三处数字相同但格式不同——

| 位置 | 格式 | 例子 |
|---|---|---|
| 客户编号 | `KY-` + 数字 | `KY-001` |
| 产品编号 | `样品-` + 数字 | `样品-001` |
| 产品料号 | `01000` + 数字 | `01000001` |

以前要一个个改，输多了容易漏。现在**只输一次数字**，三处一起写。

### 用法

命令行输入：

```
TKE
```

**第 1 步 · 自动找图框**

程序会扫描**全部布局**里的块，找出带「料号」或「图名」属性的图框，并打印一行诊断：

```
      扫描：块参照 12 个，带属性 3 个，认出图框 3 个
```

- **找到 1 个** → 直接用
- **找到多个**（多页图纸常见）→ 问你怎么处理：

```
      找到 3 个图框。
      [回车=全部一起改 / 点选=只改点中的那个 / A=取消]:
```

  - **回车** = 3 个图框一起改，**共几页 / 第几页 按左右顺序自动编号**（第1个=1、第2个=2…），不再单独问
  - **点选** = 只改点中的那一个（含 共几页 / 第几页 提问）
- **一个都没找到** → 让你点选；如果诊断显示「带属性 0 个」，程序会把图纸里的块名、
  或带属性块里的标记名打印出来，方便判断模板对不对

**第 2 步 · 输入料号数字**

```
[1/2] 公司料号数字（只输数字，如 001）<001>:
      -> 客户编号 KY-001
      -> 产品编号 样品-001
      -> 产品料号 01000001
```

- 直接回车 = 保持原来的料号不动
- 只能输**纯数字**，输了别的会提示重输
- 默认值是从图框里现有的产品料号反推出来的

**第 3 步 · 其它字段（可选）**

```
[2/2] 还要改其它字段吗？[回车=不改（只更新料号）/ Y=逐个改]: Y
      图名（客户料号）<P00000000_20260101>:
      材质 <C1020>:
      负责人（首次发行）<姓名>:
      样品数量（只输数字，单位 pcs）<3>:
      版本 <A>:
      共几页 <3>:
      第几页 <1>:
```

**每一项直接回车 = 保持原值。** 只想改其中一两项，就一路回车过去。

> **样品数量只输数字** —— 输入 `5` 会自动写成 `5pcs`，不用自己打单位。
> **图纸日期不在本命令修改**（按需求已移除），要改日期请在 CAD 里自己改。

**第 4 步 · 确认写入**

```
      ---------- 将要写入 ----------
        客户编号 = KY-001   （原 KY-001）
        产品编号 = 样品-001   （原 样品-001）
        产品料号 = 01000001   （原 01000001）
      ------------------------------
      确认写入？[回车=写入 / N=取消]:
```

只会列出**真正有变化**的字段。一个都没变会直接说「没有任何字段需要修改」。
写入后按一次 `U` 可以整体撤销（程序把这次全部改动合成了一步）。

### 常见疑问

- **提示「没自动找到图框」？** 说明图纸里没有带「料号 / 图名」属性的块，或它在别的布局里。直接点选图框即可。
- **料号前缀不是 01000？** 目前固定 `01000`，只输后面的序号（如 `001`）。
- **改错了？** 按 `U` 整体撤销。
- **量产图能用吗？** 暂时不能，图框属性结构不一样，以后单独做。

## 圆弧平滑度

打开图纸时自动把圆/圆弧平滑度设为 20000，让曲线显示更圆滑。如需在其他电脑上保留这个效果，需要自行保存图纸。

## 字体

工具包已内置常用字体（宋体、黑体、仿宋、楷体、华文细黑、工程符号字体等），装插件功能时会自动安装（需管理员权限，已装过的会自动跳过，重启不丢）。也可在"一键安装.cmd"菜单选 **[9] 字体安装** 单独补装。

客户图纸中文显示"??"通常是缺华文细黑字体，本包已内置，装完重启 CAD 即可正常显示。

## 卸载

双击"取消自动加载.cmd"，重启 CAD 即可。只会移除本工具包的自动加载设置，不影响已安装的字体，也不会删除图纸。
